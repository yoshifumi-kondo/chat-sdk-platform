diff --git a/dist/index.js b/dist/index.js
index e9ed311f326d0331458297a3e2d566ae029b9907..d1b3773308303ead480bc84d5378aaa2d2284528 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -370,9 +370,8 @@ var DiscordAdapter = class {
    * Handle incoming Discord webhook (HTTP Interactions or forwarded Gateway events).
    */
   async handleWebhook(request, options) {
-    const bodyBuffer = await request.arrayBuffer();
-    const bodyBytes = new Uint8Array(bodyBuffer);
-    const body = new TextDecoder().decode(bodyBytes);
+    const body = await request.text();
+    const bodyBytes = new Uint8Array(new TextEncoder().encode(body));
     const gatewayToken = request.headers.get("x-discord-gateway-token");
     if (gatewayToken) {
       if (gatewayToken !== this.botToken) {
@@ -396,7 +395,7 @@ var DiscordAdapter = class {
     const signature = request.headers.get("x-signature-ed25519");
     const timestamp = request.headers.get("x-signature-timestamp");
     const signatureValid = await this.verifySignature(
-      bodyBytes,
+      body,
       signature,
       timestamp
     );
@@ -474,7 +473,7 @@ var DiscordAdapter = class {
         this.publicKey
       );
       if (!isValid) {
-        const bodyString = new TextDecoder().decode(bodyBytes);
+        const bodyString = typeof bodyBytes === "string" ? bodyBytes : new TextDecoder().decode(bodyBytes);
         this.logger.warn(
           "Discord signature verification failed: invalid signature",
           {
